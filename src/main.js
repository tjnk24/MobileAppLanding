import { iframeResizerContentWindow } from 'iframe-resizer'; // eslint-disable-line no-unused-vars
import style from './main.pcss'; // eslint-disable-line no-unused-vars

window.addEventListener('load', () => document.getElementById('download-link').click());